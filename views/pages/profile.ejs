  <%-include('../partials/header.ejs')%>




<% if(user){ %>
</br>
</br>

</br>
</br>
</br>
</br>
  <div class="container-fluid mt-5">
    <div class="row mb-3">
        <div class="col-lg-4">
            <div class="card mb-3 bg-dark">
                <div draggable="false" class="card-body text-center shadow">
                    <img draggable="false" class="rounded-circle mb-3 mt-4"
                        src="https://cdn.discordapp.com/avatars/<%= user.id %>/<%= user.avatar %>" width="160" height="160">
                    <div class="mb-3"><a><%= user.username %>#<%= user.discriminator %></a></div>
                </div>
            </div>
        </div>
        <div class="col">
                <div class="card shadow mb-4 bg-dark">
                <div class="card-header py-3 bg-transparent">
                    <center>
                        <h6 class="text-primary font-weight-bold m-0">Hesap Bilgileri</h6>
                    </center>
                </div>
                <div class="card-body">
                    <h4 class="small font-weight-bold">Kullanıcı Adı<span class="float-end"><%= user.username %></span>
                    </h4>
                    <h4 class="small font-weight-bold">ID<span class="float-end"><%= user.id %></span>
                        </h4>
                </div>
            </div>
        </div>
    </div>
    <div class="col-lg-4 text-center">
        <div class="card shadow mb-4 bg-dark">
            <div class="card-header py-3 bg-transparent">
                <center>
                    <h6 class="text-primary font-weight-bold m-0">Eklediginiz Linkler </h6>
                </center>
            </div>
            <div class="card-body">
                <% if (blogs.length > 0) { %>
                    <ul>
                      <% blogs.forEach(blog => { %>
                        <li><%= blog.link %></li>
                      <% }); %>
                    </ul>
                  <% } else { %>
                    <p>Henüz bir site eklemediniz.</p>
                  <% } %>
            </div>
        </div>
    </div>
</div>
</div>
<% } else {%>
    <div class="container d-flex align-items-center justify-content-center vh-100">
        <div class="card text-center">
          <div class="card-body bg-dark">
            <h5 class="card-title">Hata!</h5>
            <p class="card-text">Profilinizi Görmek İçin Giriş Yapmalısınız !</p>
            <a class="button1" style="margin-right: 20px; filter: invert(1);" href="/callback">Giriş Yap</a>
          </div>
        </div>
      </div>

        <%}%>


        <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
        <script src="https://cdn.jsdelivr.net/npm/popper.js@1.16.0/dist/umd/popper.min.js"></script>
        <script src="https://cdnjs.cloudflare.com/ajax/libs/twitter-bootstrap/4.6.0/js/bootstrap.min.js"></script>



        <%- include("../partials/footer.ejs") %>
